{
  "name": "vCards",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js && cd vcf && zip -r vcf.zip * && mv vcf.zip ../vcards.all.zip && cd .. && rm -r vcf",
    "test": "node test.js"
  },
  "repository": "git@github.com:metowolf/vCards.git",
  "author": "metowolf <i@i-meto.com>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "chalk": "^2.4.2",
    "glob": "^7.1.4",
    "image-size": "^0.7.4",
    "is-png": "^2.0.0",
    "js-yaml": "^3.13.1",
    "pretty-bytes": "^5.2.0",
    "read-chunk": "^3.2.0",
    "vcards-js": "^2.10.0"
  }
}
